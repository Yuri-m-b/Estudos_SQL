-- Seleciona todas as colunas da tabela person.Person
SELECT *
FROM person.Person;

-- Seleciona a coluna Title da tabela person.Person
SELECT FirstName, MiddleName, LastName 
FROM person.Person; 

-- Seleciona todas as colunas da tabela person.EmailAddress
SELECT *
FROM person.EmailAddress;